import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#000',
      light: '#fff'
    },
    secondary: {
      main: '#fff',
      light: '#fff'
    },
    my: {
      main: '#fdac33',
      light: "#ccc"
    }

  }
})